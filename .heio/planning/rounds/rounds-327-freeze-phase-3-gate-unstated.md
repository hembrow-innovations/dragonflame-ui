---
id: "rounds-327-freeze-phase-3-gate-unstated"
title: "Freeze Phase 3 gate unstated"
kind: round
sitting_kind: planning
status: published
tags: [afk-plan]
created_at: "2026-09-12T05:12:35Z"
updated_at: "2026-09-12T05:12:35Z"
---

# Freeze Phase 3 gate unstated

Counterpart is the product peer. Notebook is this round.

Pick: GRAIN Phase 3 gate unstated under [[location-47-after-desktop]]. Sprint `mobile-after-desktop` may freeze. [[slice-76-desktop-vsync-window]] is met. Do not freeze Desktop first or Funding. Do not restage [[slice-300-desktop-first]], [[slice-320-funding]], [[slice-80-ios-counter]], [[slice-81-android-counter]], [[slice-82-store-binaries]], [[slice-83-talk-and-measure]], or [[slice-84-platform-view-hatch]]. Do not rewrite a location destination.

## Vault pack

Query: this is working when Phase 3 work stays behind desktop honesty. The overview Phase 3 bullet has no already-true, after-human-decision, if-native-funded, or optional words.
Area: after-desktop

Must read:

- `.heio/planning/intent.md`
- `.heio/planning/roadmap.md`
- `.heio/planning/locations/location-47-after-desktop.md`
- `.heio/planning/sprints/mobile-after-desktop/shape.md`
- `.heio/archive/planning/sprints/native-if-funded/slice-76-desktop-vsync-window.md`
- `docs/specs/ui-framework/desktop-embedder/purpose.md`
- `docs/specs/ui-framework/desktop-embedder/contract.md`
- `docs/overview/overview-ui-framework.md`

Related:

- `.heio/planning/locations/location-19-mobile-embedders.md`
- `.heio/planning/locations/location-39-desktop-embedder.md`
- `.heio/planning/rounds/rounds-01-chart-framework.md`
- `docs/architecture/architecture-layer-cake.md`
- `.heio/planning/sprints/mobile-after-desktop/slice-300-desktop-first.md`
- `.heio/planning/sprints/mobile-after-desktop/slice-320-funding.md`

Excluded: scribble; archive except slice-76; Desktop first and Funding already sliced; restaging slice-80 through slice-84; WebView shells; Expo OTA; inventing Phase 3 already-true, after-human-decision, if-native-funded, or optional words

Next: freeze grain Phase 3 gate unstated on location-47-after-desktop. Do not write `docs/specs/` in this sitting. Empty after-desktop ladder: first drain task asserts purpose, contract, and test from the location destination plus `docs/`.

No packer script exists. Assembled by hand. Open product questions on the overview are none. after-desktop spec folder does not exist. [[task-301-spec-after-desktop]] will create it for Desktop first only. [[task-321-spec-funding-after-desktop]] extends it for Funding.

## Round 1

### Questions

1. **Next grain**: What vertical cut under [[location-47-after-desktop]] this sitting freezes.
2. **Named set**: Which names `docs/` and the location already locked, and which smallest reversible defaults this sitting asserts.
3. **Ladder**: Whether an after-desktop spec folder exists.
4. **Repeat**: Whether desktop or mobile no-WebView oracles already live elsewhere.
5. **Wait**: What stays unnamed.

### Answers

1. **Next grain**: Freeze Phase 3 gate unstated. Done: Phase 3 work stays behind desktop honesty. The overview Phase 3 bullet has no already-true, after-human-decision, if-native-funded, or optional words. Bet: try not inventing a Phase 3 gate; pivot if mobile is scheduled as if the gate were written. Desktop first, Funding, and existing mobile slices stay out.
2. **Named set**: Desktop honesty is met [[slice-76-desktop-vsync-window]]. Native is funded on [[purpose-desktop-embedder]]. Overview Phase 3 is an unqualified work list. Smallest reversible defaults: no public `Phase3Gate` or `mayStartPhase3` type. No `docs/specs/ui-framework/phase-3-gate/` area. Spec area is `after-desktop`, extended after [[task-301-spec-after-desktop]] and [[task-321-spec-funding-after-desktop]]. Test path is `tests/after-desktop/`. Do not edit [[contract-desktop-embedder]]. Do not invent Phase 3 already-true, after-human-decision, if-native-funded, or optional words. All tasks `mode: afk`.
3. **Ladder**: None. [[task-301-spec-after-desktop]] writes purpose, contract, and test for Desktop first. [[task-321-spec-funding-after-desktop]] extends it for Funding. This sitting does not write specs. First Phase 3 gate unstated drain task extends that ladder after those specs.
4. **Repeat**: Do not repeat desktop vsync, window, no-WebView, or no-JS-engine oracles. Those live on [[purpose-desktop-embedder]]. Do not re-prove Native is funded. Do not restage Desktop first, Funding, or iOS, Android, store binaries, talk-and-measure, or platform-view hatch oracles.
5. **Wait**: Restaging [[slice-300-desktop-first]], [[slice-320-funding]], and [[slice-80-ios-counter]] through [[slice-84-platform-view-hatch]]. WebView shells. Expo OTA. A public Phase 3 gate token. A second gate crate. A `phase-3-gate/` spec area. Inventing Phase 3 already-true, after-human-decision, if-native-funded, or optional words.

### Candidate A

Unstated Phase 3 gate. Phase 3 work stays behind desktop honesty. No gate type. No qualifier words on the overview Phase 3 bullet.

#### Problem

[[location-47-after-desktop]] already named this surface: Phase 3 gate unstated, not a type. The grain is working when Phase 3 work stays behind desktop honesty. [[overview-ui-framework]] Phase 3 lists iOS and Android embedders, thin Xcode and Gradle shells, triples, store packaging, accessibility, text, and platform views, with no already-true, after-human-decision, if-native-funded, or optional words. The nested bet is try not inventing a Phase 3 gate; pivot if mobile is scheduled as if that gate were written. Sibling [[slice-300-desktop-first]] already froze Desktop first and listed this grain as a non-goal, with no public `AfterDesktop` or `mayStartMobile`. Sibling [[slice-320-funding]] froze Funding the same way, with no public `NativeFunded` or `mayPursueMobile`, and forbade those Phase 3 qualifier words. Native is funded on [[purpose-desktop-embedder]]. Parent [[location-19-mobile-embedders]] still waits on after desktop honesty. [[task-301-spec-after-desktop]] will write `docs/specs/ui-framework/after-desktop/` for Desktop first only. This grain must not grow an embedder, must not edit [[contract-desktop-embedder]], must not restage [[slice-80-ios-counter]] through [[slice-84-platform-view-hatch]] or the sibling slices, must not rewrite the location destination, and must not write those qualifier words onto Phase 3.

#### Usage

Callers are later mobile work and AFK drain, not app code. They do not import a Phase 3 gate, do not call `mayStartPhase3()`, and they do not restage iOS or Android counters. They schedule Phase 3 work only behind desktop honesty, and they leave the overview Phase 3 bullet unqualified. First drain writes purpose, contract, and test from the grain destination plus `docs/`, or the smallest reversible default. Suggested Done: Phase 3 work stays behind desktop honesty.

#### Shape

Public surface is the unstated Phase 3 gate only. It hides that Phase 3 has no written gate while work still waits on desktop honesty via met [[slice-76-desktop-vsync-window]]. No public gate token, matching the siblings’ bans. Sequencing lives as one after-desktop promise, not on [[contract-desktop-embedder]], not in a `phase-3-gate/` spec area, and not inside the Desktop first or Funding cuts. Ownership is this one slice under sprint `mobile-after-desktop` and the Phase 3 gate unstated grain on [[location-47-after-desktop]]. Empty ladder. No runtime start flag. No new embedder. No second crate. CHECK: in-sprint Phase 3 work names the desktop-honesty wait, and the overview Phase 3 bullet stays unqualified. EXPECT: Phase 3 work stays behind desktop honesty. Wait list: restaging [[slice-300-desktop-first]], [[slice-320-funding]], and [[slice-80-ios-counter]] through [[slice-84-platform-view-hatch]]; WebView shells; Expo OTA; public `Phase3Gate` or `mayStartPhase3`; Phase 3 already-true, after-human-decision, if-native-funded, or optional words; editing [[contract-desktop-embedder]].

#### Red flags

A public `Phase3Gate` or `mayStartPhase3` pass-through invents the gate the bet forbids and is shallow leakage. Split mint-gate then allow-mobile modules are temporal decomposition. Writing already-true, after-human-decision, if-native-funded, or optional onto the Phase 3 bullet rewrites [[overview-ui-framework]] and leaks sibling grain policy. Folding this into Desktop first or Funding mixes grains. Restaging [[slice-80-ios-counter]] through [[slice-84-platform-view-hatch]] as this gate mixes cuts. A `docs/specs/ui-framework/phase-3-gate/` area or second crate is an empty pass-through. Putting this promise on [[contract-desktop-embedder]] leaks desktop-embedder.

#### Next implementation step

First AFK drain task extends after-desktop with this unstated-gate promise from that Done sentence plus `docs/`, after [[task-301-spec-after-desktop]].

### Candidate B

Own the unqualified Phase 3 bullet on [[overview-ui-framework]]. No Phase 3 gate.

#### Problem

The Phase 3 gate unstated grain under [[location-47-after-desktop]] is working when Phase 3 work stays behind desktop honesty. [[overview-ui-framework]] already lists that work as an unqualified Phase 3 bullet: iOS and Android embedders, thin Xcode and Gradle shells, triples, store packaging, accessibility, text, platform views. Nested bet: try not inventing a Phase 3 gate; pivot if mobile is scheduled as if the gate were written. Destination stays: mobile starts only after the desktop embedder is honest. [[slice-300-desktop-first]] already locked honesty sequencing with no public `AfterDesktop` or `mayStartMobile`. [[slice-320-funding]] already locked pursuit-if-funded and forbade writing already-true, after-human-decision, if-native-funded, or optional words onto Phase 3. A dedicated after-desktop Phase 3 gate area would be a second public surface for a rule the overview already owns by leaving Phase 3 unqualified.

#### Usage

Callers are later mobile work and the AFK drain, not app code. They read Phase 3 on [[overview-ui-framework]] as a work list, not a gated phase. They do not import a Phase 3 gate, do not call `isPhase3Gated()`, and do not take a Phase 3 token as a mobile constructor argument. Thin shells at `hosts/ios/` and `hosts/android/` keep linking `crates/embedder`. A sequencing test fails if that Phase 3 bullet gains already-true, after-human-decision, if-native-funded, or optional words, if a public Phase 3 gate type appears, or if in-sprint Phase 3 work is scheduled as if that gate were written. First drain writes purpose, contract, and test from the grain destination plus `docs/`, or the smallest reversible default. Suggested Done: Phase 3 work stays behind desktop honesty.

#### Shape

No new Phase 3 gate product area and no public gate type. Own the absence of qualifier words where it already lives on [[overview-ui-framework]]. Hide whether Phase 3 is a gate at all: it is work, and start sequencing stays the already-frozen Desktop first promise on [[slice-300-desktop-first]], not a fourth phase gate callers learn. Do not restage that honesty oracle. Do not put Phase 3-start promises onto [[contract-desktop-embedder]]. Do not mint `docs/specs/ui-framework/phase-3-gate/`. Empty ladder until drain; [[task-301-spec-after-desktop]] writes after-desktop for Desktop first only. No second crate. All tasks `mode: afk`. Wait list: restaging [[slice-80-ios-counter]] through [[slice-84-platform-view-hatch]]; WebView shells; Expo OTA; a public Phase 3 gate token; editing [[contract-desktop-embedder]]; rewriting the location destination.

#### Red flags

Reject a public `Phase3Gate` or `isPhase3Gated()` pass-through. Reject a dedicated Phase 3 gate spec area for a rule the overview already owns by remaining unqualified. Reject putting mobile-start promises onto [[contract-desktop-embedder]]. Reject split invent-gate then allow-mobile modules. Reject writing the four qualifier words onto Phase 3. Reject restaging mobile counters. Reject repeating desktop vsync, window, no-WebView, or no-JS-engine oracles. Reject folding this into Funding or rewriting Desktop first.

#### Next implementation step

Write the empty ladder for this grain, then a red test that [[overview-ui-framework]] Phase 3 stays unqualified and Phase 3 work is not scheduled as if a gate were written, with no Phase 3 gate type.

## Synthesis

Base is Candidate A. Graft from B: no public `Phase3Gate` or `mayStartPhase3` type; the unqualified Phase 3 bullet stays on [[overview-ui-framework]]; no `docs/specs/ui-framework/phase-3-gate/` area; do not edit [[contract-desktop-embedder]]; no second crate; do not restage the Desktop first honesty oracle.

Reject B as the slice shape: owning only the overview absence mixes the after-desktop sequencing cut with cold-reader overview. Phase 3 work staying behind desktop honesty is an after-desktop grain.

Tradeoffs accepted:

- We accept a spec-and-sequencing slice, not a new host, in exchange for locking the unnamed Phase 3 gate unstated grain without restaging mobile counters.
- We accept no public gate type in exchange for citing the unqualified Phase 3 bullet where it already lives.
- We accept extending `after-desktop` after [[task-301-spec-after-desktop]] and [[task-321-spec-funding-after-desktop]] in exchange for not minting a second Phase 3 gate area.
- We accept one oracle on `tests/after-desktop/` in exchange for not repeating desktop or iOS no-WebView oracles.

Alternatives considered:

- Public `Phase3Gate` API: shallow pass-through, lost.
- Writing already-true, after-human-decision, if-native-funded, or optional onto Phase 3: invents the gate, lost.
- `docs/specs/ui-framework/phase-3-gate/` area: second surface for an unstated rule, lost.
- Mobile-start promises on [[contract-desktop-embedder]]: mobile is out of scope there, lost.
- Restaging [[slice-80-ios-counter]] as this grain: mixes cuts, lost.

Open questions and risks:

- None the product peer cannot answer from [[location-47-after-desktop]], [[location-19-mobile-embedders]], [[purpose-desktop-embedder]], [[overview-ui-framework]], and [[intent]].

Next implementation step: extend after-desktop with the unstated-gate promise after [[task-321-spec-funding-after-desktop]], then red-green Phase 3 work stays behind desktop honesty.

### Tracer bullets

1. Spec unstated-gate promise on after-desktop. blocked_by: [[task-321-spec-funding-after-desktop]]. AFK. Purpose, contract, and test only. No product code. Do not rewrite Desktop first or Funding promises.
2. Red-green Phase 3 work stays behind desktop honesty. blocked_by: spec. AFK. `tests/after-desktop/phase-3-work-stays-behind-desktop-honesty.test.mjs`. Overview Phase 3 stays unqualified. No public `Phase3Gate` type. Do not repeat desktop or iOS no-WebView oracles.

## Confirm

Confirmed.
