---
id: "rounds-303-freeze-not-the-public-site"
title: "Freeze not the public site"
kind: round
sitting_kind: planning
status: published
tags: [afk-plan]
created_at: "2026-09-12T12:10:00Z"
updated_at: "2026-09-12T12:10:00Z"
---

# Freeze not the public site

Counterpart is the product peer. Notebook is this round.

Pick: GRAIN Not the public site under [[location-21-git-package]]. Sprint `framework-in-draconic` may freeze. Do not restage [[slice-69-importable-package]] name, library-product, library-first, or no-empty-crate oracles. Do not rewrite a location destination.

## Vault pack

Query: this is working when the public site stays TanStack Start and is not a rewrite destination or a prototype of this framework.
Area: ui-framework/git-package

Must read:

- `.heio/planning/intent.md`
- `.heio/planning/roadmap.md`
- `.heio/planning/locations/location-21-git-package.md`
- `.heio/planning/sprints/framework-in-draconic/shape.md`
- `docs/specs/ui-framework/git-package/purpose.md`
- `docs/specs/ui-framework/git-package/contract.md`
- `docs/overview/overview-ui-framework.md`
- `docs/overview/glossary.md`

Related:

- `.heio/planning/locations/location-17-web-component-library.md`
- `.heio/planning/locations/location-22-crate-layout.md`
- `.heio/planning/rounds/rounds-01-chart-framework.md`
- `docs/specs/ui-framework/git-package/test.md`

Excluded: scribble; no packer in package.json; no blocking slice; architecture-layer-cake not named by this location See also

Next: freeze grain Not the public site on location-21-git-package. Do not write `docs/specs/`; ladder exists.

No packer script exists. Assembled by hand. Open product questions are none.

## Round 1

### Questions

1. **Next grain**: What vertical cut under [[location-21-git-package]] this sitting freezes.
2. **Named set**: Which names `docs/` and the location already locked, and which smallest reversible defaults this sitting asserts.
3. **Ladder**: Whether a git-package spec folder exists.
4. **Repeat**: Whether public-site or git-package identity oracles already live elsewhere.
5. **Wait**: What stays unnamed.

### Answers

1. **Next grain**: Freeze Not the public site. Done: the public site stays TanStack Start and is not a rewrite destination or a prototype of this framework. Pivot if that site is treated as the first app of this library. Working name, library product, library-first, and no empty crates stay out.
2. **Named set**: Locked [[contract-git-package]] `git-package.identity:forbid-public-site-rewrite`. Intent We will not Public TanStack site. [[purpose-git-package]] Out of scope Answer 10. Smallest reversible defaults: no public fence helper, no `PublicSite` type, no `app.config` export. CHECK lives in `tests/git-package/`. Do not edit [[contract-git-package]]. All tasks `mode: afk`.
3. **Ladder**: Exists at `docs/specs/ui-framework/git-package/`. First task does not write purpose, contract, or test.
4. **Repeat**: Do not repeat name, library-product, library-first, or no-empty-crate oracles. Those live on [[slice-69-importable-package]] and [[purpose-git-package]]. Do not restage in-sprint framework-source, host-descriptor, ticker, or vsync work.
5. **Wait**: Rewriting the public site. Treating it as the first app. A public fence API. Editing the locked promise. Restaging [[slice-69-importable-package]].

### Candidate A

Git package identity fence. Existing contract promise. No new module.

#### Problem

[[location-21-git-package]] already named this nested grain: the public site stays TanStack Start and is not a rewrite destination or a prototype of this framework. Parent destination stays the dragonflame-ui git package in this repo, not a language feature. [[intent]] We will not, [[purpose-git-package]] Out of scope Answer 10, and [[contract-git-package]] `git-package.identity:forbid-public-site-rewrite` already lock that fence. Open product questions are empty. This sitting names that grain only. It must not mint a site API, must not treat the site as the first app, and must not duplicate in-sprint framework-source, host-descriptor, ticker, or vsync work.

#### Usage

Callers keep `import("dragonflame-ui")` from this checkout. They do not import the public site, do not take TanStack Start config, and do not open this repo as a Start app. Planning and AFK drain ask one question: is this library being used as a rewrite of that site. The answer is no. CHECK stays the existing test `public site stays TanStack Start and is not a rewrite destination` in `tests/git-package/git-package.test.mjs`. Pivot only if a sitting treats that site as the first app of this library.

#### Shape

Public surface is the git package identity fence, the promise `git-package.identity:forbid-public-site-rewrite` on [[contract-git-package]]. No new public name. Ownership is this nested grain under [[location-21-git-package]] and docs area `docs/specs/ui-framework/git-package/`. The surface hides where the public site lives, how Start is wired there, and any rewrite plan. Callers still see only the library product. Do not edit the locked promise. Do not add `app.config`. Do not restage name, library-product, library-first, or no-empty-crate oracles.

#### Red flags

A public `forbidPublicSiteRewrite()` or `isTanStackStartApp()` helper is a shallow pass-through over the existing `app.config` absence check. Re-exporting Start config or site routes leaks a product this repo does not own. Split detect-then-forbid modules are temporal decomposition. Folding this grain into framework `.drac` source or a first-app prototype mixes cuts.

#### Next implementation step

Name the slice CHECK at the existing public-site oracle in `tests/git-package/git-package.test.mjs`; add no module and do not edit [[contract-git-package]].

### Candidate B

Site-boundary owner in git-package tests. Same locked promise. Deeper prove.

#### Problem

The nested grain on [[location-21-git-package]] is a fence, not a framework feature: the public site stays TanStack Start and is not a rewrite destination or a prototype of this library. [[purpose-git-package]] already puts a site rewrite out of scope. [[contract-git-package]] already locks `git-package.identity:forbid-public-site-rewrite`. [[test-git-package]] currently points at a root `app.config` absence in `tests/git-package/git-package.test.mjs`. That CHECK leaks a Start filename and does not own how a site app would be recognized in this package, nor that dragonflame-ui is not that site's renderer. The bet pivots if the site is treated as the first app of this library. A promise plus a two-filename CHECK at the repo root will miss an in-workspace site app or library-as-site-renderer wiring.

#### Usage

Library callers do not change. They still `import` dragonflame-ui. They never import a fence helper, a Start type, or a site path.

The only caller is the git-package identity sitting.

- The CHECK asks one owner to prove this checkout is not the public site and not a prototype of it.
- A later in-repo `apps/` or example folder is rejected by that same owner if it is a site app whose renderer is this library.
- `package.json` still names dragonflame-ui and still exports the library. No site entry is added.

#### Shape

Keep the locked promise name. Do not add a public framework API.

Ownership cut: one site-boundary owner under the git-package spec area, used only by tests. It owns three facts as one policy.

- Identification: the public site is not this git package. This checkout's product is the library. A public-site app is not a workspace member and not the first package.
- Renderer non-ownership: this library is not the public site's renderer. No in-tree site app mounts dragonflame-ui as its UI.
- Not a rewrite destination: this package does not stage a Start-to-dragonflame-ui rewrite of that site.

Start markers stay private to that owner. Callers do not coordinate find, then assert Start, then assert renderer.

The public surface stays the git package and `git-package.identity:forbid-public-site-rewrite`. Hidden: how a site app is recognized, how renderer wiring would look, and that the first app of this library must not be the public site. Exposed to library consumers: nothing new.

Does not rewrite the public site. Does not require a sibling site checkout. Does not export Start types. Does not invent a public `PublicSite` name.

#### Red flags

Rejected shallow shape: a pass-through that only checks root `app.config` files. That is temporal, leaky, and shallow.

This cut is not a pass-through: the owner completes identification, renderer non-ownership, and not-first-app together. Not temporal: those are one policy, not three modules. Not leakage: Start filenames and site-app shapes do not become library exports or new contract names.

Forbidden strawman not used: no export of TanStack Start config types.

#### Next implementation step

Replace the root `app.config` existence asserts in `tests/git-package/git-package.test.mjs` with one git-package site-boundary prove that this checkout is the library package, contains no public-site app, and does not wire dragonflame-ui as that site's renderer.

## Synthesis

Base is Candidate A. Graft from B: CHECK is one site-boundary prove that this checkout is the library package, contains no public-site app, and does not wire dragonflame-ui as that site's renderer. Start filenames stay private to the test owner. No public fence helper. Do not edit [[contract-git-package]].

Reject B as the slice shape: a named site-boundary product module is extra surface. Keep ownership in git-package tests.

Tradeoffs accepted:

- We accept a fence slice, not a new module, in exchange for naming the unnamed Not the public site grain without restaging [[slice-69-importable-package]].
- We accept no public fence type in exchange for hiding Start markers and site-app shape behind the test owner.
- We accept deepening the existing named test in exchange for not leaking two root filenames as the whole oracle.

Alternatives considered:

- Public `forbidPublicSiteRewrite` API: shallow pass-through, lost.
- Root `app.config` absence as the whole CHECK: leaky and shallow, lost as the sole prove.
- Editing [[contract-git-package]]: promise already locked, lost.
- Restaging [[slice-69-importable-package]] as this grain: mixes cuts, lost.

Open questions and risks:

- None the product peer cannot answer from [[location-21-git-package]], [[purpose-git-package]], [[contract-git-package]], and [[intent]].

Next implementation step: deepen the named public-site test, then red-green the fence without editing the contract.

### Tracer bullets

1. Prove public site stays TanStack Start. blocked_by: none. AFK. `tests/git-package/git-package.test.mjs` named test `public site stays TanStack Start and is not a rewrite destination`. One site-boundary prove: this checkout is the library package, contains no public-site app, and does not wire dragonflame-ui as that site's renderer. Do not edit [[contract-git-package]]. No public fence helper. Do not restage name, library-product, library-first, or no-empty-crate oracles.

## Confirm

Confirmed.
