---
description: Alias for /afk-roadmap — audit exactly one ROADMAP.md row, file tickets, exit. Does not implement.
agent: build
argument-hint: "[Roadmap ID | continue | status]"
---

Load **roadmap-audit** and **management**. Run one sitting, then exit.

Arguments: $ARGUMENTS

Same command as `/afk-roadmap`. Findings only. One Roadmap row. Tickets for gaps. No implement. No second row.
