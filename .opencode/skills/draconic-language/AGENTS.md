# Draconic language reference

Reference only. Prefer `SKILL.md` stack notes and `rules/<id>.md`.

Do not load this file unless you are stuck and the router plus two or three rules were not enough.

This pack is the knowledge home for toolchain conventions. Workflow stays in sibling skills. Load them; do not copy their bodies here:

- `draconic-loop` one Roadmap atom
- `tdd` red-green
- `gauntlet-loop` implement-and-verify
- `diagnose` hard bugs
- `docs` / `domain-modeling` glossary and ADRs
- `codebase-design` depth and seams

Source of truth outside this folder:

- Glossary: `CONTEXT.md`
- Locked decisions: `docs/adr/`
- Completeness: `ROADMAP.md` plus the Conformance suite
- Frontend entry: `crates/draconic-frontend/src/lib.rs`
